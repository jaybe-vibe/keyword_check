version: "3.8"

services:
  keyword-check:
    build: .
    container_name: keyword_check
    restart: unless-stopped
    ports:
      - "8502:8502"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - TZ=Asia/Seoul
